pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus' version "${quarkusPlatformVersion}"
    }
}
rootProject.name = 'bttr-server'
